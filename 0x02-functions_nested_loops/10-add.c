#include <stdio.h>
#include "main.h"
/**
 * add - a function to add two integers
 * @x: a single number
 * @y: a single number
 * Return: sum of integers
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
